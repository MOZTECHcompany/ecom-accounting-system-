# 售後系統 `523792c` 與遠端後續修正審核

審核日期：2026-09-02

## 基準與結論

- 使用者指定 `523792cdb1c07a324cfa2e52722eb1b145f3d8d4` 為完整功能保留基線。
- `origin/main=6ed5d6d14ea7db3449820621ccbffca570394077` 比基線多 70 個 commits、影響 97 個檔案。
- ERP 整併不直接 merge 這 70 個 commits；每項變更依下表轉成 ERP 的資料契約、狀態規則、權限或低干擾 UI 驗收項目。
- 直接存取舊 Product、保固資料庫、NextAuth 使用者與 ECPay 憑證的程式不移植；改由 ERP 既有商品、權限、發票與對帳服務負責。

## 必須吸收的資料契約

| 遠端變更 | ERP 目的契約 | 處理方式 |
|---|---|---|
| `6d48ea9` 錯買來回件收款 | 來回件可有獨立應收、付款與正向換貨出貨 | 原生重建 |
| `820161a` 綠界替代發票號碼欄位 | provider response 保留原始欄位並正規化正式發票號碼 | 併入 ERP Invoicing |
| `34e441d`、`a08f5e6`、`ae92725` 作廢／重開／同步 | 發票開立、作廢、重開是分離且可追溯的 provider 命令 | 併入 ERP Invoicing |
| `e19a2a3` 保固來源 | 案件引用可追溯保固來源，不由瀏覽器直連另一資料庫 | 建 ERP 唯讀查詢 API |
| `49c0f08` 來源窗口與維修逆物流 | 來源通路改用受控主檔；維修保留逆物流 | 建 ERP master + workflow |
| `1e147bb` ERP 商品搜尋 | 案件商品引用 ERP Product ID 並保存快照 | 直接用 ERP Product API |
| `fafaec8`、`a6f915b`、`6ed5d6d` 維修應收金額同步 | 應收金額只有一個 domain source，案件編輯必須重算未完成付款請求 | 原生重建與測試 |
| `e76e0b4` 收款銀行資料 | 收款帳號由 ERP 受控設定提供，不硬編在舊前端 | 併入 ERP 設定 |
| `76d878d` 綠界失敗內容 | 保存 provider code、message、reference 與 retry 狀態 | 併入 ERP Invoicing |
| `76653f2` 原始訂單號 | 案件保存並顯示 SalesOrder 關聯與來源訂單號 | 原生欄位 |
| `751b6d0` 物流修正稽核 | 物流更正不可覆蓋無痕，必須有 before / after / actor | 共用 Audit |
| `b2dd287`、`33cadab` 業務角色與帳號層級 | 工作角色與管理層級分離 | 對應 ERP RBAC |

發票買受人與載具欄位是遠端 schema 的必要增量：

- `PaymentSubmission.buyerTaxId`
- `PaymentSubmission.carrierType`
- `PaymentSubmission.carrierNumber`
- `InvoiceRecord.buyerTaxId`
- `InvoiceRecord.carrierType`
- `InvoiceRecord.carrierNumber`

來回件必要增量：付款區域、末五碼、收款帳號、幣別／匯率／運費／最終金額，以及正向換貨出貨草稿。這些欄位必須進 ERP 目的模型與 migration importer。

## 必須吸收的狀態與金流規則

- `63c79c9`：來回件完成必須以倉庫實際收件為證據。
- `463f976`、`f022e83`、`65fe6ff`、`4371929`：會計、倉儲與維修動作由角色或模組權限控制，不以畫面可見性代替後端授權。
- `bcb879b`：補寄建立正式出貨後才可自動結案；若出貨建立失敗不得先關閉。
- `e09647f`、`1b45594`、`32e4eb5`、`d000461`、`20b5df7`、`49b3f1c`：物流匯出／匯入必須可往返、保留備註與來源通路，且批次行為與單筆狀態機一致。
- `ce25486`、`3c470fc`：退款派車在派車前留在客服待辦；免退款案件收到退貨後才可結案。
- `2ba0ef1`：付款提醒延期後必須重簽連結與到期時間。
- `9a41483`、`0147e7b`、`9e9ae6d`、`b530d30`、`0143d91`、`3567428`：零元／不需發票／不收款是明確規則，不以缺資料猜測；付款與發票 gate 分離。
- `116364d`：不需付款的案件編輯也必須同步正式狀態，不可只更新畫面。
- `ab9b6e4`、`8c6f9e3`：維修收件需進檢測流程並保留逆物流，不使用表單勾選直接宣告已收件。
- `4a90fd6`：案件關閉不代表失去發票補開能力；補開仍須權限、provider 證據與 Audit。
- `0143d91`、`26133b3`、`7368a8c`：不收費與測試案件的發票排除需使用正式旗標；過寬排除必須可復原，不能靠文字模糊判斷。
- `2803c34`：維修報價確認後仍需進付款流程，不可跳過實際收款。

## 權限與工作台驗收

- `9adb083`、`b2c64f5`、`06df994`：建立／編輯案件由 ERP 後端角色與權限共同驗證。
- `e2dc521`、`32b693e`、`85b2b17`、`70c6b36`、`8341ba6`、`84f9b7c`：維修權限是能力，不建立多餘導覽入口；維修人員只看與操作授權範圍。
- `c5f173e`、`713e8cc`：會計工作台可切換查看完成紀錄，預設待辦維持乾淨。
- `9d8e394`、`34ac686`、`9418824`：名稱與導覽採單一「售後管理」，角色工作隊列放在頁內，不重複側邊選單。

## UI 只保留驗收目標、不直接搬程式

以下 commits 的價值是低干擾介面驗收，不直接複製舊 Next.js 畫面：

- `3074229`、`b27215f`、`005ab67`：案件詳情、清單動作與逆物流卡片保持緊湊。
- `f4908b4`：不能操作時只顯示一個明確原因，不放長篇建議文字。
- `9d90c1a`：部署紀錄本身不構成功能規格，僅供追溯。
- `31fc023`、`35232f8`、`50f16a4`：移除可由流程推導的勾選與重複欄位，避免表單互相矛盾。
- `acdfb1f`、`5e5bc88`、`46f80ff`：逾期層級與付款狀態以短標籤呈現。

## 70 個 commits 覆蓋清單

下列每個遠端 commit 均已歸入上方資料契約、狀態規則、權限或 UI 驗收；沒有任何項目因「較新」而直接合併：

`63c79c9`, `3074229`, `b27215f`, `005ab67`, `820161a`, `463f976`, `f022e83`, `f4908b4`, `65fe6ff`, `4371929`, `6d48ea9`, `34e441d`, `a08f5e6`, `bcb879b`, `e09647f`, `1b45594`, `32e4eb5`, `ce25486`, `3c470fc`, `e19a2a3`, `49c0f08`, `9d90c1a`, `1e147bb`, `2ba0ef1`, `d000461`, `9a41483`, `0147e7b`, `9e9ae6d`, `b530d30`, `b2dd287`, `9d8e394`, `c5f173e`, `9adb083`, `b2c64f5`, `0143d91`, `06df994`, `26133b3`, `7368a8c`, `33cadab`, `713e8cc`, `fafaec8`, `3567428`, `116364d`, `ab9b6e4`, `e76e0b4`, `76d878d`, `4a90fd6`, `76653f2`, `31fc023`, `8c6f9e3`, `35232f8`, `acdfb1f`, `50f16a4`, `20b5df7`, `5e5bc88`, `34ac686`, `46f80ff`, `e2dc521`, `751b6d0`, `2803c34`, `49b3f1c`, `32b693e`, `85b2b17`, `ae92725`, `a6f915b`, `9418824`, `70c6b36`, `8341ba6`, `84f9b7c`, `6ed5d6d`.

## 下一個建構關卡

1. 在遠端 HEAD 的隔離 worktree 移植同一份唯讀 exporter，加入遠端 schema 新欄位，避免部署 `523792c` 造成正式功能倒退。
2. ERP 建立 staging 與 source key / checksum；先匯入候選，不寫正式售後、庫存或財務表。
3. 以 613 / 499 筆基準與各子表筆數做 dry-run 核對，輸出 75 筆未對應商品 queue。
4. 通過重跑新增 0 筆後，才開始 ERP 原生案件狀態機與單一寫入切換。
